allprojects {
	group = "net.poundex.jtop"
	version = "0.1.0-SNAPSHOT"
}

subprojects {
	apply plugin: "java"
	apply plugin: "groovy"

	repositories {
		mavenLocal()
		jcenter()
	}

	dependencies {
		compile group: 'org.codehaus.groovy', name: 'groovy-all', version: '2.4.13'
	}
}
